# Dashboard UI Library - مكتبة واجهات التحكم

مكتبة واجهات تحكم جاهزة للاستخدام في مشاريع Laravel، توفر تصميم عصري ومتجاوب مع جميع أحجام الشاشات.

## المميزات ✨

- ✅ **تصميم عصري وجذاب** - واجهة مستخدم عصرية بألوان متناسقة
- ✅ **متجاوب بالكامل** - يعمل بشكل مثالي على جميع الأجهزة (سطح المكتب، التابلت، الجوال)
- ✅ **قائمة جانبية قابلة للطي** - يمكن طيها وفتحها بسهولة مع حفظ الحالة
- ✅ **شريط علوي ثابت** - يحتوي على البحث والإشعارات وقائمة المستخدم
- ✅ **سهل التكامل** - يمكن دمجه بسهولة في أي مشروع Laravel
- ✅ **نظام إشعارات** - نظام إشعارات مدمج جاهز للاستخدام
- ✅ **أكواد نظيفة ومنظمة** - مع تعليقات توضيحية بالعربية والإنجليزية

## البنية التنظيمية 📁

```
project-root/
├── public/
│   └── dashboard-ui/
│       ├── css/
│       │   └── dashboard.css      # ملف الأنماط الرئيسي
│       └── js/
│           └── dashboard.js      # ملف JavaScript الرئيسي
└── resources/
    └── views/
        └── layouts/
            └── app.blade.php      # ملف التخطيط الأساسي
```

## خطوات التثبيت والتكامل 🚀

### 1. نسخ الملفات

انسخ المجلدات والملفات التالية إلى مشروع Laravel الخاص بك:

```bash
# نسخ ملفات CSS و JavaScript
cp -r public/dashboard-ui/ /path/to/your/laravel/project/public/

# نسخ ملف التخطيط
cp resources/views/layouts/app.blade.php /path/to/your/laravel/project/resources/views/layouts/
```

### 2. استخدام التخطيط في صفحاتك

في أي صفحة Blade تريد استخدام لوحة التحكم، قم بتوسيع التخطيط الأساسي:

```blade
@extends('layouts.app')

@section('title', 'عنوان الصفحة')
@section('page-title', 'عنوان الصفحة')
@section('page-subtitle', 'وصف الصفحة')

@section('content')
    <div class="dashboard-card">
        <div class="card-header">
            <h3 class="card-title">بطاقة مثال</h3>
        </div>
        <div class="card-body">
            <p>محتوى البطاقة هنا...</p>
        </div>
    </div>
@endsection
```

### 3. تخصيص القائمة الجانبية

يمكنك إضافة عناصر جديدة للقائمة الجانبية باستخدام `@section('sidebar-nav')`:

```blade
@extends('layouts.app')

@section('sidebar-nav')
    <div class="sidebar-nav-item">
        <a href="{{ url('/custom-page') }}" class="sidebar-nav-link">
            <span class="sidebar-nav-icon">⭐</span>
            <span class="sidebar-nav-text">صفحة مخصصة</span>
        </a>
    </div>
@endsection

@section('content')
    <!-- محتوى الصفحة -->
@endsection
```

## الاستخدام المتقدم 💡

### استخدام نظام الإشعارات

المكتبة تحتوي على نظام إشعارات مدمج يمكن استخدامه بسهولة:

```javascript
// إشعار نجاح
showSuccess('تم الحفظ بنجاح!');

// إشعار خطأ
showError('حدث خطأ أثناء الحفظ');

// إشعار تحذير
showWarning('يرجى التحقق من البيانات');

// إشعار معلومات
showInfo('معلومة مهمة');
```

### تخصيص الألوان

يمكنك تخصيص الألوان من خلال تعديل المتغيرات CSS في ملف `dashboard.css`:

```css
:root {
    --primary-color: #6366f1;      /* اللون الأساسي */
    --primary-dark: #4f46e5;       /* اللون الأساسي الداكن */
    --success-color: #10b981;      /* لون النجاح */
    --danger-color: #ef4444;       /* لون الخطأ */
    /* ... المزيد من المتغيرات */
}
```

### إضافة أنماط مخصصة

يمكنك إضافة أنماطك الخاصة في ملفات Blade الخاصة بك:

```blade
@extends('layouts.app')

@push('styles')
    <style>
        /* أنماطك المخصصة هنا */
        .custom-class {
            /* ... */
        }
    </style>
@endpush

@section('content')
    <!-- محتوى الصفحة -->
@endsection
```

### إضافة سكريبتات مخصصة

```blade
@extends('layouts.app')

@push('scripts')
    <script>
        // سكريبتاتك المخصصة هنا
        console.log('Dashboard loaded!');
    </script>
@endpush
```

## المكونات المتاحة 🧩

### البطاقات (Cards)

```blade
<div class="dashboard-card">
    <div class="card-header">
        <h3 class="card-title">عنوان البطاقة</h3>
    </div>
    <div class="card-body">
        محتوى البطاقة
    </div>
</div>
```

### الفئات المساعدة (Utility Classes)

```blade
<!-- الألوان -->
<span class="text-primary">نص أساسي</span>
<span class="text-success">نص نجاح</span>
<span class="text-danger">نص خطأ</span>

<!-- الخلفيات -->
<div class="bg-primary">خلفية أساسية</div>
<div class="bg-secondary">خلفية ثانوية</div>

<!-- الظلال -->
<div class="shadow-sm">ظل صغير</div>
<div class="shadow-md">ظل متوسط</div>
<div class="shadow-lg">ظل كبير</div>

<!-- الزوايا المستديرة -->
<div class="rounded">زوايا مستديرة</div>
<div class="rounded-lg">زوايا مستديرة كبيرة</div>
```

## التخصيص والتوسيع 🔧

### تخصيص القائمة الجانبية

يمكنك تعديل عناصر القائمة الجانبية مباشرة في ملف `app.blade.php`:

```blade
<div class="sidebar-nav-item">
    <a href="{{ url('/your-route') }}" class="sidebar-nav-link">
        <span class="sidebar-nav-icon">🎯</span>
        <span class="sidebar-nav-text">عنصر جديد</span>
    </a>
</div>
```

### تخصيص الشريط العلوي

يمكنك إضافة عناصر جديدة للشريط العلوي في قسم `navbar-right`:

```blade
<div class="navbar-item">
    <button class="navbar-icon-btn" aria-label="Custom Action">
        <span>🔧</span>
    </button>
</div>
```

## المتغيرات CSS المتاحة 🎨

جميع المتغيرات متاحة في `:root` ويمكن تخصيصها:

- **الألوان الأساسية**: `--primary-color`, `--secondary-color`
- **ألوان الحالة**: `--success-color`, `--warning-color`, `--danger-color`, `--info-color`
- **الألوان المحايدة**: `--bg-primary`, `--bg-secondary`, `--text-primary`, `--text-secondary`
- **المسافات**: `--spacing-xs` إلى `--spacing-2xl`
- **أبعاد القائمة الجانبية**: `--sidebar-width`, `--sidebar-collapsed-width`
- **الظلال**: `--shadow-sm` إلى `--shadow-xl`
- **الزوايا المستديرة**: `--radius-sm` إلى `--radius-xl`

## المتطلبات 📋

- Laravel 8.x أو أحدث
- متصفح حديث يدعم CSS Grid و Flexbox
- خط Inter (يتم تحميله تلقائياً من Google Fonts)

## الدعم والمساعدة 💬

إذا واجهت أي مشاكل أو لديك استفسارات:

1. تأكد من أن المسارات صحيحة في ملف `app.blade.php`
2. تأكد من أن ملفات CSS و JS موجودة في المجلدات الصحيحة
3. تحقق من console المتصفح للأخطاء JavaScript
4. تأكد من أن Laravel يخدم الملفات الثابتة بشكل صحيح

## الترخيص 📄

هذه المكتبة متاحة للاستخدام الحر في مشاريعك الشخصية والتجارية.

## التحديثات القادمة 🚀

- [ ] إضافة المزيد من المكونات (جداول، نماذج، إلخ)
- [ ] دعم الوضع الداكن (Dark Mode)
- [ ] إضافة المزيد من الأيقونات
- [ ] تحسين الأداء والسرعة

---

**صُنع بـ ❤️ للمجتمع العربي**
